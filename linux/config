[PaStA]
### Project name. Should not contain whitespaces ###
PROJECT_NAME = "Linux"
MODE = "mbox"

### necessary for upstream comparison. Patches within this range will be considered ###
UPSTREAM = "v2.6.39..origin/master"

### Blacklist of upstream commit hashes to ignore for upstream comparison ###
UPSTREAM_BLACKLIST = "linux-blacklist"

### Project specific autothresholds for interactive rating ###
INTERACTIVE_THRESHOLD = 1.1
AUTOACCEPT_THRESHOLD = 0.82
DIFF_LINES_RATIO = 0.4
MESSAGE_DIFF_WEIGHT = 0.3

### Used for parsing mailinglists ###
[mbox]
MINDATE = 2011-05-01
MAXDATE = 2030-12-31

# default: false
# USE_PATCHWORK_ID = true

[mbox.raw]
# "hoster" = ["listname", "filename"]
# ...

[mbox.pubin]
# Mailing lists are hosted somewhere. A ML host may host multiple lists.
# To define lists of a host, those lists need to be defined within the list of
# lists of a ML host:
#
# "hoster" = ["listname1",  "listname2", ...]
#
# Shards (0.git, 1.git, ...) of public inboxes MUST be located under
# resources/project_name/resources/mbox/pubin/hoster/listname1/ . PaStA
# will then try to automatically find shards beginning from 0.

"alsa-project.org" = ["alsa-devel"]

"lists.infradead.org" = [
	"linux-amlogic",
	"linux-arm-kernel",
	"linux-i3c",
	"linux-mtd",
	"linux-riscv",
]

"lists.linuxfoundation.org" = ["ksummit-discuss"]

"lists.ozlabs.org" = [
	"linuxppc-dev",
]

"systeme.lip6.fr" = ["cocci"]

"vger.kernel.org" = [
	"linux-spdx",
	"linux-next",
	"linux-kernel",
	"linux-bluetooth",
	"linux-block",
	"linux-btrfs",
	"linux-cifs",
	"linux-clk",
	"linux-crypto",
	"linux-ext4",
	"linux-fsdevel",
	"linux-hwmon",
	"linux-iio",
	"linux-integrity",
	"linux-media",
	"linux-mips",
	"linux-modules",
	"netdev",
	"linux-nfs",
	"linux-parisc",
	"linux-pci",
	"linux-renesas-soc",
	"linux-rtc",
	"linux-security-module",
	"linux-sgx",
	"linux-trace-devel",
	"linux-wireless",
	"linux-watchdog",
]
